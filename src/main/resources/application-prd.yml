server:
  port: 6820

spring:
  #mysql
  datasource:
    url: ${MYSQL_URL}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  #jpa
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        type: trace

#variables
jwt:
  signer_key:

mail:
  username:
  password: